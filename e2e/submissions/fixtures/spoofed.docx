This is plain text pretending to be a DOCX by file extension only.
It has no DOCX (ZIP) magic number, so server-side validation must reject it
even though the client accept filter (.docx) lets it through.
